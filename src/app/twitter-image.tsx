// Twitter cards use the same image as Open Graph by re-exporting the
// opengraph-image route. Next.js auto-generates the twitter:image
// metadata when this file exists.
export { default, alt, size, contentType } from "./opengraph-image";
